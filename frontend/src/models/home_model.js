export default class HomeModel {
    constructor(nextTours, mostPopularCities, suggestedTours) {
        this.nextTours = nextTours;
        this.mostPopularCities = mostPopularCities;
        this.suggestedTours = suggestedTours;

    }
}